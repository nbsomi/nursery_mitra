name: nursery_mitra
description: Nursery Mitra - Mobile data-feeding client
publish_to: 'none'

version: 1.0.15

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  geolocator: ^10.1.0
  camera: ^0.10.5
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  url_launcher: ^6.2.6
  package_info_plus: ^8.0.0
  geocoding: ^4.0.0
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
